### A Chrome Extension in ReactJs

The code has implementation of a basic extension and how to scrape data. Need to integrate the two to achieve our desired functionality.

Navigate to the scrape directory, install dependencies and then run

```npm run start```


